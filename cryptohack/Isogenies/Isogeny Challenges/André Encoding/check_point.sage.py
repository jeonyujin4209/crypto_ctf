

# This file was *autogenerated* from the file check_point.sage
from sage.all_cmdline import *   # import sage library

_sage_const_37 = Integer(37); _sage_const_2 = Integer(2); _sage_const_64 = Integer(64); _sage_const_1 = Integer(1); _sage_const_256 = Integer(256); _sage_const_0 = Integer(0); _sage_const_3 = Integer(3)
import json
proof.all(False)

p = _sage_const_37  * _sage_const_2 **_sage_const_64  * lcm(range(_sage_const_1 , _sage_const_256 )) - _sage_const_1 
F = GF(p**_sage_const_2 , modulus=[_sage_const_1 , _sage_const_0 , _sage_const_1 ], names=('i',)); (i,) = F._first_ngens(1)
E = EllipticCurve(F, [_sage_const_0 , _sage_const_1 ])

with open("output.txt") as f:
    ct = json.load(f)

item = ct[_sage_const_0 ]
x = F(item["P"]["x"][_sage_const_0 ]) + F(item["P"]["x"][_sage_const_1 ]) * i
y = F(item["P"]["y"][_sage_const_0 ]) + F(item["P"]["y"][_sage_const_1 ]) * i
# Test on original E0 (y^2 = x^3 + 1)
print(f"y^2 == x^3+1 ? {y**_sage_const_2  == x**_sage_const_3  + _sage_const_1 }")
# Phi goes to some NEW curve, not E0! The image phi(P), phi(Q) is on the codomain.
# So we need to find the codomain curve. For Weil pairing we don't need the curve
# equation if we just use the image points — but the EllipticCurve constructor
# requires the points to be on a known curve.
# Alternative: compute Weil pairing WITHOUT constructing points on E.
# E.weil_pairing(P, Q, n) requires P, Q on E.
# But we could construct E_new satisfying the point equation.
# Actually since each ciphertext entry has a DIFFERENT codomain, we'd need to
# compute the short Weierstrass for each. Reconstruct from two points:
#   y1^2 = x1^3 + a*x1 + b1
#   y2^2 = x2^3 + a*x2 + b2
# 2 unknowns (a, b), 2 equations → solve.
x1, y1 = x, y
x2 = F(item["Q"]["x"][_sage_const_0 ]) + F(item["Q"]["x"][_sage_const_1 ]) * i
y2 = F(item["Q"]["y"][_sage_const_0 ]) + F(item["Q"]["y"][_sage_const_1 ]) * i
# y1^2 - y2^2 = x1^3 - x2^3 + a*(x1 - x2)
# a = (y1^2 - y2^2 - x1^3 + x2^3) / (x1 - x2)
a_coef = (y1**_sage_const_2  - y2**_sage_const_2  - x1**_sage_const_3  + x2**_sage_const_3 ) / (x1 - x2)
b_coef = y1**_sage_const_2  - x1**_sage_const_3  - a_coef * x1
print(f"codomain a = {a_coef}")
print(f"codomain b = {b_coef}")
E_new = EllipticCurve(F, [a_coef, b_coef])
P_new = E_new(x1, y1)
Q_new = E_new(x2, y2)
print(f"constructed P_new = {P_new}")
print(f"E_new order: {E_new.order()}")


# TLS Handshake (15pts)
## TLS Part 1: The Protocol

This challenge uses the same packet capture as the previous challenge. Let's describe the communication we see at a high level.The TLS communication only begins at packet 10, but what happens before provides important context: Packets 1-2: First, when we typed cryptohack.org into the address bar and

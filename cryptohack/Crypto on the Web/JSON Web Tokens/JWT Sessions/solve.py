# JWT Sessions: theory question
# "the flag is the name of the HTTP header used by the browser to send JWTs to the server"
# JWTs are sent in the Authorization HTTP header (Bearer token scheme)
print("Flag: Authorization")

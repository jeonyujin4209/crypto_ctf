# Curves and Logs (40pts)
## Starter

The Elliptic Curve Discrete Logarithm Problem (ECDLP) is the problem of finding an integer nnn such that Q=[n]PQ = [n]PQ=[n]P.Like we encountered with the discrete logarithm problem, scalar multiplication of a point in E(Fp)E(\Fp)E(Fp​) seems to be be a hard problem to undo, with the most efficient 

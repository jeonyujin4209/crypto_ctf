# Honest Verifier Zero Knowledge (30pts)
## Sigma Protocol

The previous challenge showed that this protocol satisfies Special Soundness! That is, if P can convince V that he knows www with non-negligible probability, then we are able to efficiently "extract" www from P, which shows that www (or material to efficiently compute the www) "exists" somewhere ins
